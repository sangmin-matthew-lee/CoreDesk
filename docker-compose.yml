version: '3.8'

services:
  web:
    image: ${DOCKER_IMAGE_NAME:-coredesk:latest}
    ports:
      - "80:3000"
    volumes:
      - ./data:/app/data
    restart: always
    environment:
      - NODE_ENV=production
